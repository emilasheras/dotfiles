# dotfiles
files for preseeding OS fresh installs after first boot 

---

```sh
curl -fsSL https://github.com/emilasheras/dotfiles/releases/latest/download/ubuntu-bootstrap.sh -o ubuntu-bootstrap.sh
chmod +x ubuntu-bootstrap.sh
./ubuntu-bootstrap.sh
```
